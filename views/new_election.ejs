<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="../public/css/style.css">
    <title><%= title %></title>
</head>
<style>
    body {
       background:url("images/signup1.jpg");
       background-repeat: no-repeat;
       background-size: cover;
       width: 100%;
    }
 </style>
<body >
    <%- include('header.ejs') %>
    <div class="flex gap-3 mx-10 mt-6 bg-gray-50">
        <span>></span>
        <a href="/elections" class="text-black-800 hover:text-black-600"> CreatedElections</a>
        <span>></span>
    </div>
    <div class=" flex antialiased flex-col overflow-hidden bg-gray-50 py-6 sm:py-12">
        <div class=" py-3 sm:w-96 mx-auto text-center">
    
            <div>
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-blue-100 my-3 list-none rounded-md px-4 py-2"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
            <div class="mt-4 bg-pink shadow-md rounded-lg text-left">
                <span class="text-2xl font-semibold flex justify-center">Create a new election</span>
                <form action="/elections" method="post" class="px-8 py-6">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                    <label for="electionName" class="block mt-3 font-semibold">What is your election's name?</label>
                    <input id="electionName" name="electionName" type="text" placeholder="Name" class="border w-full h-5 px-3 py-5 mt-2 hover:outline-none focus:outline-none focus:ring-indigo-500 focus:ring-1 rounded-md" required>
                    <label for="urlString" class="block mt-3 font-semibold">Enter a custom URL string (With no spaces)</label>
                    <input id="urlString" name="urlString" type="text" placeholder="URL String" class="border w-full h-5 px-3 py-5 mt-2 hover:outline-none focus:outline-none focus:ring-indigo-500 focus:ring-1 rounded-md" required>
                    <button type="submit" class="mt-6 w-full bg-blue-500 text-white py-2 px-6 rounded-md hover:bg-purple-600 ">Create</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>